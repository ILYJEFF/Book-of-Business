/** Preset labels for email rows (custom text allowed via "Custom…" in the UI). */
export const EMAIL_LABEL_PRESETS = ['Work', 'Personal', 'Other'] as const

/** Preset labels for phone rows (typical roles including mobile and landline). */
export const PHONE_LABEL_PRESETS = [
  'Mobile',
  'Work',
  'Cell',
  'Landline',
  'Home',
  'Main',
  'Fax',
  'Other'
] as const

export const CUSTOM_LABEL_OPTION = 'Custom…'
